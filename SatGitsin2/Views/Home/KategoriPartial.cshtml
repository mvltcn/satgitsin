@model IEnumerable<SatGitsin2.Models.Kategori>

@{ 
    Layout = null;
}

<div class="btn-group">
    <div class="btn-group btn-group-justified" role="group" aria-label="...">

        @foreach (var item in Model)
        { 
            <div class="btn-group" role="group" >
                <button type="button" onclick="location.href = '/Home/IlanKategori/@item.KategoriId'; " class="btn btn-default">@item.KategoriAdi</button>
            </div>
         
        }
    </div>
</div>



@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.KategoriAdi)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.KategoriAdi)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.KategoriId }) |
                @Html.ActionLink("Details", "Details", new { id=item.KategoriId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.KategoriId })
            </td>
        </tr>
    }

    </table>*@
